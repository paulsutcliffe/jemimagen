#region-servicios.container
  .region
    .linea
    .region-general
      %h2.titulo-general
        SLOGAN DE JEM IMAGEN
      %p.texto-generl
        Far far away, behind the word mountains, far from the countries Vokalia and Consonantia, there live the blind texts. Separated they live in Bookmarksgrove right at the coast of the Semantics, a large language ocean.
        Far far away, behind the word mountains, far from the countries Vokalia and Consonantia, there live the blind texts. Separated they live in Bookmarksgrove right at the coast of the Semantics, a large language ocean.
      .img-down
        = image_tag image_path ('arrow-down.jpg')
  .clear
  .region-servicios
    - if current_admin
      .admin-control
        = link_to 'Crear Servicio', new_servicio_path, class: 'btn btn-primary'
    
    - @servicios.each do |servicio|
      .servicio
        = image_tag servicio.foto_url(:thumb)
        %p= servicio.nombre
        - if current_admin
          = link_to 'Editar', edit_servicio_path(servicio), class: 'btn btn-success'
          = link_to 'Eliminar', servicio, :method => :delete, :data => { :confirm => '¿Estás Seguro?' }, class: 'btn btn-danger'
